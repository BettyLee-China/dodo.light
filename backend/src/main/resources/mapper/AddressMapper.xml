<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.demo.light.repository.AddressMapper">
    <select id="selectAddressById" resultType="Address">
        select * from user_address
        where id=#{id} and user_id=#{userId}
    </select>
    <insert id="insertAddress" parameterType="Address">
        insert into user_address(
                                id,user_id,receiver_name,phone,province,city,district,
                                 detail_address,is_default,created_at,updated_at
        )
        values (
                #{id},#{userId},#{receiverName},#{phone},#{province},#{city},#{district},
                #{detailAddress},#{isDefault},#{createdAt},#{updatedAt}
               )
    </insert>
    <select id="selectDefaultAddress" resultType="Address">
        select *
        from user_address
        where is_default = true and user_id=#{userId}
    </select>

    <select id="selectAllByUserId" resultType="Address">
        select * from user_address
        where user_id=#{userId}
    </select>

    <update id="setDefault">
        update user_address
        set is_default = true
        where id=#{id}
    </update>
    <update id="cancelDefault">
        update user_address
        set is_default = false
        where id=#{id}
    </update>

</mapper>